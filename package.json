{
  "name": "stencil-nextjs-integration",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "npm run build:stencil && npm run build:nextjs",
    "build:stencil": "cd packages/component-library && npm run build",
    "build:nextjs": "cd packages/nextjs-app && npm run build",
    "clean": "lerna clean",
    "start": "lerna exec --parallel -- npm run dev",
    "test": "lerna run test",
    "test:watch": "lerna exec -- npm test -- --watch"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
